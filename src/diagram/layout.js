// Backward-compat shim. Real implementation lives in ./layout/index.js.
// Existing imports `from '../diagram/layout.js'` keep working unchanged.
export { computeLayout, routeArrow } from './layout/index.js';
